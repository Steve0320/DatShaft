<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shaftware.shaftquack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.shaftware.MainActivity"
            android:label="ShaftQuack">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name="com.shaftware.SignInActivity"
            android:label="Login" />
        <activity
            android:name="com.shaftware.ConversationActivity"
            android:label="Display Chat" />
        <activity
            android:name="com.shaftware.ChannelsActivity"
            android:label="Channels" />
        <activity android:name="com.shaftware.SettingsActivity" />
        <activity
            android:name="com.shaftware.SplashScreen"
            android:label="Shaft Quack">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.shaftware.AddChannelActivity"
            android:label="Add Channel" />
    </application>

</manifest>